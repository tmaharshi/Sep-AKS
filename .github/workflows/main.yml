name: Build and Deploy image to ACR
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@main
      - run: |
          docker build -t sampleapp:1:0:0 application code
          docker images
